require 'nokogiri'
require 'twilio-ruby'
require_relative 'lib/THEPROJECT.rb'

task :text_dumb do 
  hi = Scrapper.new
  lol = Texter.new

  lol.call(number, hi.scrape)
  #open file numbers.txt, for each number in numbers.txt call number
end
